import React from "react";
import Canvas from "./components/canvas";

/**
 * Main App component for the MNIST demo application.
 * @returns {JSX.Element} The main App component that renders the Canvas.
 */
function App() {
  return (
    <Canvas />
  );
}

export default App;
